# Python
.venv/
__pycache__/
*.pyc

# Full-resolution source images (local archive only — the site ships
# optimized variants from site/assets; keep this folder backed up separately)
catalog_src/

# Editor
.vscode/
